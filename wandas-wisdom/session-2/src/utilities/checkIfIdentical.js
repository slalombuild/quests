function checkIfIdentical(firstString, secondString) {
    let isIdentical = false

    if (firstString === secondString) {
        isIdentical = true
    }

    return isIdentical
}

export default checkIfIdentical
